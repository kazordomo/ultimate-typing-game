import styled from 'react-emotion';

export default styled('button')({
    padding: '10px 15px',
    fontSize: '18px',
    fontWeight: '700',
    backgroundColor: 'skyblue',
    border: 'none',
    borderRadius: '4px',
    outline: 'none',
    cursor: 'pointer'
});